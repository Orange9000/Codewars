var anarchy = function () {
  return 'I can write numbers like, ' + [null,null,null,null].map((e,i)=>i)
                                                             .filter(e=>e!=false)
                                                             .join(', ') +'.';
}
